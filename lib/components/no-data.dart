import 'package:flutter/material.dart';

class NoListData extends StatelessWidget {
  const NoListData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("No record found",
    ),);
  }
}
